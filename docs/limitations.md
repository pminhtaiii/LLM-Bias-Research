# Limitations

- BBQ reflects U.S. English-speaking cultural contexts and stereotypes.
- Model behavior can depend on prompt, runtime, quantization, and reasoning configuration.
- Bias scores must be interpreted with accuracy.
- GPT results are currently reported aggregates rather than independently rescored per-example outputs.
- Qwen is pending repaired rescoring and is intentionally excluded from current headline results.
