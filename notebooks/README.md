# Original notebooks

These notebooks are retained as provenance for the workflows that were actually supplied by the group:

- `qwen_inference.ipynb`
- `qwen_scoring_original.ipynb`
- `gpt_scoring_original.ipynb`

The canonical public result tables are generated by `scripts/build_results.py`, not by relying on the original notebooks unchanged.
